### Task venue: Resolve the venue
**State:** ingest-venue

Resolve the venue reference `{{conference}}` — a website URL, a local file,
or a venue name — into `inputs/venue.md` (scope, review process, selection
criteria). Fill `inputs/guidelines.md` and `inputs/review-form.md` from the
venue's published material wherever they carry the
`<!-- venue-intake: fill -->` marker; leave user-supplied content untouched.
