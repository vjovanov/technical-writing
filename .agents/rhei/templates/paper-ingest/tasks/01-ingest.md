### Task ingest: Ingest {{paper_id}}
**State:** ingest

Read the submission at `{{paper}}` and produce the canonical reading of it.

Ingest mode: `{{source_kind}}`. Page rendering resolution: {{render_dpi}} DPI.
